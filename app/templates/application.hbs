<div class="container-fluid">
    <nav id="sidebar">
        <ul>
            <li class="title"><h3>Pages</h3></li>
            {{#each pages as | page | }}
            <li>{{#link-to page.route }}{{page.name}}{{/link-to}}</li>
            {{/each}}
        </ul>
    </nav>
    {{outlet}}
</div>